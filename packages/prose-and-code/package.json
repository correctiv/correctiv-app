{
  "name": "@correctiv/prose-and-code",
  "description": "Helpers for the checks that hold a repository's prose to its code: a walk that cannot pass on nothing, a two-sided ratchet, a drift check, comment stripping, a number read out of a sentence, and a parse for the literals a person reads. Node and TypeScript, no framework, no test runner of its own.",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "typescript": "~6.0.3"
  },
  "scripts": {
    "typecheck": "tsc --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run"
  }
}
